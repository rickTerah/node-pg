language: node_js

node_js:
  "stable"
  
cache:
  directories: 
    "node_modules"

script:
  - npm run test

install:
  - npm install

services:
  - postgresql

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres